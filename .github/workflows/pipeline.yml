name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Clean
      run: ./gradlew clean
    - name: Compile
      run: ./gradlew assemble
    - name: Test
      run: ./gradlew check
    - name: Sonar
      run: ./gradlew sonarqube

